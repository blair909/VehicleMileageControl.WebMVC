@model IEnumerable<VehicleMileageControl.Model.MaintenanceListItem>
<!DOCTYPE html>
<html>
@{
    ViewBag.Title = "Index";
}

<head>
    <link href="~/Views/Shared/main.css" rel="stylesheet" type="text/css" />
</head>
<body id="one">
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaintenanceId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OdomoterMileage)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MaintenanceId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OdomoterMileage)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }

    </table>
</body>
</html>